{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [
    ".env"
  ],
  "pipeline": {
    "build": {
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        "dist/**"
      ]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "dependsOn": [
        "framework-compare-mitosis#build"
      ],
      "env": [
        "CREATE_REACT_APP_ENDPOINT",
        "SPOTIFY_API_ENDPOINT",
        "REACT_APP_SPOTIFY_API_ENDPOINT"
      ]
    }
  }
}
